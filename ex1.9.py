import math

angulo = float(input("digite um número: "))

rad = math.radians(angulo)

seno = math.sin (rad)
cosseno = math.cos (rad)
tangente = math.tan (rad)

print (f"O seno {seno}, o cosseno {cosseno} e a tangente {tangente}")
